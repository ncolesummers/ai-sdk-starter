# Generate a random secret: https://generate-secret.vercel.app/32 or `openssl rand -base64 32`
AUTH_SECRET=****

# Better Auth Base URL - use your network IP for remote access (e.g., http://10.0.1.161:3000)
# For local development: http://localhost:3000
# This URL is used in magic link emails and for CORS configuration
BETTER_AUTH_URL=http://localhost:3000

# The following keys below are automatically created and
# added to your environment when you deploy on vercel

# Ollama Configuration (for local AI model inference)
# Default: http://localhost:11434/v1 (note the /v1 suffix for OpenAI compatibility)
# For Kubernetes deployments, use: http://host.docker.internal:11434/v1
# For Linux hosts, you may need to use your host IP instead of host.docker.internal
OLLAMA_BASE_URL=http://localhost:11434/v1

# Tavily API Key (for web search and content extraction)
# Sign up at https://tavily.com to get your API key
# Optional: If not set, search tools will return an error
TAVILY_API_KEY=tvly-****

# Instructions to create a Vercel Blob Store here: https://vercel.com/docs/storage/vercel-blob
BLOB_READ_WRITE_TOKEN=****

# Instructions to create a PostgreSQL database here: https://vercel.com/docs/storage/vercel-postgres/quickstart
POSTGRES_URL=****


# Instructions to create a Redis store here:
# https://vercel.com/docs/redis
REDIS_URL=****
